# Business Rules
## Overview
The application will follow these business rules:
* A user can belong to multiple groups
* A group can have multiple users
* A role can be assigned to multiple users
* A user can have multiple roles