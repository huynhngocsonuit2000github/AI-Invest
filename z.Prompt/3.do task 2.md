Read the existing planning files and current source project.

Implement TASK-0002 user module only.

Requirements:

- Create Models/User.cs
- Create DTOs/CreateUserRequest.cs
- Create DTOs/UpdateUserRequest.cs
- Create DTOs/UserResponse.cs
- Update Data/AppDbContext.cs with DbSet<User>
- Create Repositories/IUserRepository.cs
- Create Repositories/UserRepository.cs
- Create Services/IUserService.cs
- Create Services/UserService.cs
- Create Controllers/UsersController.cs

User fields:

- Id: Guid
- UserName: string, required, max length 100
- Email: string, required, max length 200
- DisplayName: string, optional, max length 200
- IsActive: bool
- CreatedAtUtc: DateTime

API endpoints:

- GET /api/users
- GET /api/users/{id}
- POST /api/users
- PUT /api/users/{id}
- DELETE /api/users/{id}

Rules:

- Use async methods
- Keep controller thin
- Business logic in service
- Data access in repository
- Return 404 when user not found
- Return CreatedAtAction after create
- Register repository and service in Program.cs
- Update memory/project_map.json
- Update memory/feature_history.md
- Update memory/test_knowledge.md
- Run dotnet build for source/TodoApp.Api/TodoApp.Api.csproj

Do not implement groups, roles, todos, JWT, or tests yet.

Use only JSON tool actions. Do not explain steps.
